Now can you make an email forworder app that will send an randomly generated code which could contains (special characters,underscore,hexadecimals,., of the size of 9 words) to the given email and note that it will works on the api request on flask and in the api the user will have to provide an email. And it will also save the generated code on a SQLite table named pin_code.db with user_id and pin_code attributes where each time the server automatically generate and saves the user_id like this (Uxx).
